# Copy to .env (ignored). Existing shell/CI variables take precedence.
# Literal values only: no shell commands, variable expansion, or multiline values.
GITHUB_REPOSITORY=exnano/udara
UDARA_BUNDLE_ID=io.exnano.udara
APPLE_TEAM_ID=YOURTEAMID
SIGNING_IDENTITY='Developer ID Application: Your Name (YOURTEAMID)'
NOTARY_PROFILE=udara-notary
# Used by scripts/create-draft-release.sh:
# RELEASE_NOTES_FILE=docs/release-notes.md

# Local WAQI dataset assessment only; never embedded in the distributed app.
WAQI_TOKEN=
